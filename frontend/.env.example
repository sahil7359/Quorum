# Points the frontend at the deployed backend. No secrets here -- this is the
# only backend detail the browser ever sees, and the API itself is public and
# read-only.
NEXT_PUBLIC_API_BASE=http://localhost:8000
